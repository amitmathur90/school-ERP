# Dependencies
node_modules/
server/node_modules/

# Environment files — NEVER commit real secrets
.env
server/.env
!.env.example
!server/.env.example

# Build output
dist/
build/

# Local database files (old SQLite version, if present)
*.db
*.db-shm
*.db-wal

# Uploaded documents — local dev only; on Render these need a persistent
# disk or object storage (see README's Render deployment section)
server/uploads/

# Editor/OS
.vscode/
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
*.log
